# --- web framework ---
Django>=5.0,<6.0
python-dotenv
mysqlclient
Pillow
whitenoise

# --- RAG pipeline (kept from 3rd round) ---
faiss-cpu
numpy
pypdf
rank_bm25

# --- LLM / embeddings ---
google-genai
openai
sentence-transformers

# --- LangChain (3rd-round parallel path; remove this block if unused) ---
langchain-classic
langchain-community<0.4
langchain-core
langchain-google-genai
langchain-openai
langchain-text-splitters

# ============================================================
# Removed from the 3rd-round requirements.txt:
#   fastapi, uvicorn, python-multipart  -> replaced by Django
#   sqlalchemy, pymysql                 -> Django ORM + mysqlclient
#   pydantic, pydantic-settings         -> Django settings + Form
#   python-jose[cryptography]           -> Django session auth
#   passlib[bcrypt], bcrypt<4.1         -> Django PBKDF2 hashing
#   langchain-protocol, langsmith       -> unused
# ============================================================
