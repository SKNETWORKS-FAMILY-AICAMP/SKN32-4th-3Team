# Minimal deps for a 5-minute run (no MySQL / API key / torch needed)
#   Only for the DB_ENGINE=sqlite3  EMBEDDING_BACKEND=hash  RAG_SOURCE=files combo.
# For full features (local/openai embeddings, LangChain path, MySQL) use requirements.txt.
Django>=5.0,<6.0
python-dotenv
Pillow
faiss-cpu
numpy
pypdf
# settings.MIDDLEWARE 가 무조건 로드합니다 — 없으면 runserver 가 뜨지 않습니다.
whitenoise
# rebuild_index() 가 RAG_PIPELINE 과 무관하게 호출합니다 — 없으면 rag_reindex 가 실패합니다.
rank_bm25
